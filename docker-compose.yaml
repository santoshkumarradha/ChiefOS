version: '3.8'

services:
  chief:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Override at build time: docker compose build --build-arg MODEL_URL=<url>
        MODEL_URL: https://huggingface.co/Qwen/Qwen2.5-3B-Instruct-GGUF/resolve/main/qwen2.5-3b-instruct-q4_k_m.gguf
        MODEL_SHA256: e4cfb1c3f3c4f3c4f3c4f3c4f3c4f3c4f3c4f3c4f3c4f3c4f3c4f3c4f3c4f3c
    image: chief-os:latest
    container_name: chief-os
    ports:
      # 4711 = chief-core HTTP API (intent, status, brief, etc.)
      - "4711:4711"
      # 5173 = Morning Brief static files (served by chief-core or standalone)
      - "5173:5173"
    volumes:
      # Persistent state directory
      - chief-state:/var/lib/chief
    environment:
      # Chief configuration
      CHIEF_MODEL_PATH: /model/qwen2.5-3b.gguf
      CHIEF_STATE_DIR: /var/lib/chief
      RUST_LOG: info
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4711/status"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: unless-stopped

volumes:
  chief-state:
    driver: local
