# Chief OS PlanDB template — human-readable sidecar.
# Auto-generated by scripts/plandb-export.sh via 'plandb export'.
# Graph shape only (titles + descriptions + deps). NOT sufficient for
# full-state restore — status, results, and contexts are missing.
# For restore use docs/plandb-state.sql.

name: chief-os-w6-migrate-phase-4
description: null
tasks:
- ref_id: task-1
  title: Inspect ADR-0013 migration targets
  description: 'Read only the allowed-scope files needed for Phase 4: prototypes/hn-briefer/src/lib.rs, prototypes/hn-briefer/manifest.toml, prototypes/hn-briefer/README.md, prototypes/hn-briefer/tests/integration.rs, crates/chief-sdk/src/capability.rs, and crates/chief-sdk/Cargo.toml. Confirm exact edits and note any existing user changes. Acceptance: relevant contexts and line locations are known before editing.'
  kind: generic
  priority: 0
  parent_ref: null
  pre_condition: null
  post_condition: null
  pre_hook: null
  post_hook: null
- ref_id: task-2
  title: Apply scoped Phase 4 migration edits
  description: 'Modify only prototypes/hn-briefer/** and crates/chief-sdk/**. Replace HN Briefer ctx.llm().generate stub with ctx.ai().prompt(prompt).max_tokens(500).tier(Tier::Fast).call::<String>().await? and import chief_sdk::Tier. Update manifest grant kind to llm.ai with tier_min=fast and budget_max_tokens=500. Remove CapabilityKind::LlmGenerate variant and its description/danger_level match arms. Bump chief-sdk Cargo.toml version to 0.3.0. Update HN README grant text and migration note. Update HN integration test from LlmGenerate to LlmAi. Acceptance: requested grep targets no longer contain llm().generate or LlmGenerate, and HN source contains ctx.ai()/Tier.'
  kind: generic
  priority: 0
  parent_ref: null
  pre_condition: null
  post_condition: null
  pre_hook: null
  post_hook: null
- ref_id: task-3
  title: Run focused package gates
  description: 'Run cargo build/test/clippy for chief-sdk and hn-briefer exactly as requested. Acceptance: cargo build -p chief-sdk, cargo test -p chief-sdk, cargo build -p hn-briefer, cargo test -p hn-briefer, cargo clippy -p chief-sdk --all-targets -- -D warnings, and cargo clippy -p hn-briefer --all-targets -- -D warnings all pass, or failures are captured with actionable context.'
  kind: generic
  priority: 0
  parent_ref: null
  pre_condition: null
  post_condition: null
  pre_hook: null
  post_hook: null
- ref_id: task-4
  title: Run final workspace and grep gates
  description: 'Run cargo fmt --check, cargo build --workspace, grep -r "llm().generate\|LlmGenerate" crates/chief-sdk/src prototypes/hn-briefer/src prototypes/hn-briefer/manifest.toml expecting no matches, and grep -r "ctx.ai()\|Tier::" prototypes/hn-briefer/src expecting at least one match. Acceptance: all final gates pass and results are summarized.'
  kind: generic
  priority: 0
  parent_ref: null
  pre_condition: null
  post_condition: null
  pre_hook: null
  post_hook: null
dependencies:
- from_ref: task-1
  to_ref: task-2
  kind: feeds_into
- from_ref: task-2
  to_ref: task-3
  kind: feeds_into
- from_ref: task-3
  to_ref: task-4
  kind: feeds_into
context: []

